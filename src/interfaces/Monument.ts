export default interface MonumentIntarface {
  id: string;
  name: string;
  address?: string;
  source?: string;
  registrant_url?: string;
  adm2?: string;
  adm3?: string;
  lon: number,
  lat: number,
}
